name: event_authentication
description: Used for User and JWT management using event_db. Also has secure password storage and management.
version: 0.1.2
repository: https://github.com/VHCBlade/event_authentication

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:

  crypto: ^3.0.3
  dart_jsonwebtoken: ^2.7.1
  encrypt: ^5.0.1

  event_bloc: ^4.2.0
  event_db: ^0.1.10

  tuple: ^2.0.1

dev_dependencies:
  event_bloc_tester: ^0.1.4

  lints: ^2.0.0
  test: ^1.21.0
  very_good_analysis: ^4.0.0
